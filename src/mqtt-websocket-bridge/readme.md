# mqtt to websocket bridge

Telegraf doesn't do mqtt to websocket, so this is a simple Python script that does that instead. Currently in the docker compose using Compose Build Specification, but could also be hosted on docker hub.